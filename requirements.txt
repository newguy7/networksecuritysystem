python-dotenv
pandas
numpy
pymongo
certifi
pymongo==4.10.1
pymongo[srv]
scikit-learn
mlflow
pyaml
# dill
dagshub

fastapi
uvicorn
python-multipart

# Update all dependencies to their latest versions to ensure compatibility
# pip install --upgrade pymongo to avoid the error

# add at the end when project is done
# it will create all the packages
##-e .
